Args = -H:+ReportExceptionStackTraces \
  -H:+TraceClassInitialization \
  -H:EnableURLProtocols=https \
  -H:+StackTrace \
  -H:+AddAllCharsets \
  --enable-https \
  --enable-http \
  --enable-all-security-services \
  --no-server \
  --initialize-at-build-time=io.fabric8.kubernetes.client.internal.CertUtils$1